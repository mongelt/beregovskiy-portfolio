# Bug Fix Session

## Purpose of this command
**CRITICAL**: This command starts a bug fix session. This means that the user found a bug that needs to be fixed immediately. During the bug fix session, pay extra attention to documenting every step that you take. 

Next steps:
- Read entire `.cursor\commands\comms-review.md`
- Start a new section in the current running document for the current stage (which has planning or update in its name) in the current step (the user may indicate where exactly)
- Use the following naming convention: [Current Step].Bug.[number of the bug being fixed].[number of attempt to fix the bug]
- The Heading 2 for this new section should follow the naming convention.
- Document the current state, user's bug reports, and suggest fixes. 
**CRITICAL**: Do not make changes to code unless the user explicitly authorizes it. 
**CRITICAL**: DO NOT RUN COMMANDS UNLESS THE USER AUTHORIZES IT EXPLICITLY